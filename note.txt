1>  java中short约等于正负三万，int约等于正负三亿
2>  java中默认的十进制数字为int型，所以如果超过int型的范围，如long，需要加入L或l
3>  浮点数默认是double
4>  BigDecimal类，不产生摄入误差的精确数字计算，超过long的数字使用BigInteger类
5>  容量小的数据类型可以自动转换为容量大的数据类型（表示数的范围）； 大范围转小范围 超过小范围的数会损失精度，超出将被截断，被解释成不同的值
6>  做二元运算，将自动转换为容量大的类型； L尽量放在第一个数字后面
7>  final在常量面前表示常量，常量用大写
8>  常量：驼峰规则   常量：大写和下划线    类名：首字母大写和驼峰原则
9>  jdk7 以前 switch只能是int，枚举类型，现在可以使字符串
10> JAVA中只有值传递
11> 不要使用默认包
12> JAVADOC生成API文档 /**